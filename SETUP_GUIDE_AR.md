# 🚀 دليل الإعداد الكامل لـ MEDDELA

## المشكلة الحالية: الصفحات تظهر فارغة ❌

عندما تزور التطبيق على Vercel، تظهر لوحة التحكم (Dashboard) لكن باقي الصفحات فارغة. السبب:

1. ⚠️ **متغيرات البيئة غير مُعدّة في Vercel**
2. ⚠️ **قاعدة البيانات Supabase غير مُعدّة أو فارغة**
3. ⚠️ **لا توجد بيانات تجريبية للعرض**

---

## 📋 خطة الإصلاح الكاملة

### الخطوة 1️⃣: إعداد Supabase

#### أ) إنشاء مشروع Supabase جديد

1. اذهب إلى: https://supabase.com/dashboard
2. انقر على **"New Project"**
3. اختر اسم للمشروع: `meddela`
4. اختر كلمة سر قوية للقاعدة (احفظها!)
5. اختر المنطقة: `North Europe (Stockholm)` - الأقرب للسويد
6. انتظر 2-3 دقائق حتى يكتمل الإعداد

#### ب) نسخ بيانات الاتصال

بعد إنشاء المشروع:

1. اذهب إلى: **Settings** → **API**
2. انسخ:
   - **Project URL** (مثال: `https://xxxxx.supabase.co`)
   - **anon public** key
   - **service_role** key (سرية - لا تشاركها!)

#### ج) تشغيل قاعدة البيانات

1. في لوحة Supabase، اذهب إلى: **SQL Editor**
2. افتح محتويات الملف: `/supabase/migrations/001_initial_schema.sql`
3. انسخ **كل المحتوى** والصقه في SQL Editor
4. انقر **Run** (تشغيل)
5. انتظر حتى ينتهي (يجب أن ترى "Success" ✅)

#### د) إضافة البيانات التجريبية (Seed Data)

1. في SQL Editor، افتح الملف: `/supabase/seed.sql`
2. انسخ المحتوى والصقه
3. انقر **Run**
4. ستُضاف بيانات تجريبية (منظمة، مستخدم، جهات اتصال، قوالب SMS)

---

### الخطوة 2️⃣: إعداد متغيرات البيئة في Vercel

#### أ) الذهاب إلى إعدادات Vercel

1. اذهب إلى: https://vercel.com/dashboard
2. افتح مشروعك: `meddelamig`
3. اذهب إلى: **Settings** → **Environment Variables**

#### ب) إضافة المتغيرات التالية

أضف كل متغير على حدة، واختر **All Environments** (Production, Preview, Development):

```bash
# Supabase (إلزامي ⚠️)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# App Configuration (إلزامي)
NEXT_PUBLIC_APP_URL=https://meddelamig-mxh408t8b-riadh-massaoudi-s-projects.vercel.app
NEXT_PUBLIC_APP_NAME=MEDDELA

# 46elks SMS (اختياري - يمكن إضافته لاحقاً)
ELKS_API_USERNAME=your-username
ELKS_API_PASSWORD=your-password
ELKS_SENDER_NAME=MEDDELA
```

#### ج) إعادة نشر التطبيق

بعد إضافة المتغيرات:

1. اذهب إلى: **Deployments**
2. انقر على آخر deployment
3. انقر على القائمة (...) → **Redeploy**
4. اختر **Use existing Build Cache** = NO
5. انقر **Redeploy**
6. انتظر 2-3 دقائق

---

### الخطوة 3️⃣: إنشاء حساب مستخدم

بعد إعادة النشر:

1. اذهب إلى رابط التطبيق: https://meddelamig-mxh408t8b-riadh-massaoudi-s-projects.vercel.app
2. انقر **Register** (تسجيل)
3. أدخل:
   - الاسم الكامل
   - البريد الإلكتروني
   - كلمة السر
   - معلومات الشركة
4. بعد التسجيل، ستُنشأ منظمة جديدة لك تلقائياً
5. ستحصل على **100 SMS مجاني** للبدء!

---

### الخطوة 4️⃣: اختبار التطبيق

الآن يجب أن يعمل التطبيق بشكل كامل:

✅ **Dashboard** - يعرض الإحصائيات  
✅ **Contacts** - يمكنك إضافة جهات اتصال  
✅ **Templates** - قوالب SMS جاهزة  
✅ **Messages** - إرسال رسائل  
✅ **Campaigns** - حملات جماعية  
✅ **Analytics** - تحليلات  
✅ **Settings** - الإعدادات

---

## 🐛 استكشاف الأخطاء

### المشكلة: لا تزال الصفحات فارغة

**الحل:**
```bash
# 1. تحقق من متغيرات البيئة في Vercel
#    تأكد من أنها مُضافة بشكل صحيح

# 2. افتح Console في المتصفح (F12)
#    ابحث عن رسائل الخطأ الحمراء

# 3. تحقق من Vercel Logs:
#    Settings → Functions → View Function Logs
```

### المشكلة: "Failed to load" أو أخطاء Supabase

**الحل:**
```bash
# تحقق من:
# 1. Row Level Security (RLS) في Supabase:
#    Authentication → Policies
#    يجب أن يكون enabled

# 2. تحقق من أن الجداول موجودة:
#    Table Editor → يجب أن ترى:
#    - organizations
#    - users
#    - contacts
#    - sms_messages
#    - campaigns
#    - sms_templates
```

### المشكلة: لا يمكن التسجيل أو تسجيل الدخول

**الحل:**
```bash
# في Supabase:
# 1. اذهب إلى: Authentication → Providers
# 2. تأكد من تفعيل Email Provider
# 3. تعطيل "Confirm email" للاختبار:
#    Authentication → Settings → 
#    Email Confirmations = Disabled (للتطوير فقط)
```

---

## 📱 إعداد إرسال SMS الحقيقي (اختياري)

إذا أردت إرسال SMS حقيقية (وليس تجريبية):

### 1. إنشاء حساب 46elks

1. اذهب إلى: https://46elks.com
2. سجل حساب جديد
3. ستحصل على **$2 مجاني** للاختبار
4. اذهب إلى: **Account** → **API keys**
5. انسخ:
   - Username (API Key)
   - Password (API Secret)

### 2. إضافة بيانات 46elks في Vercel

في Vercel → Environment Variables، أضف:

```bash
ELKS_API_USERNAME=u_your_username_here
ELKS_API_PASSWORD=your_api_password_here
ELKS_SENDER_NAME=MEDDELA  # أو اسم شركتك (max 11 حرف)
```

### 3. إعادة نشر التطبيق

- Deployments → Redeploy

الآن يمكنك إرسال SMS حقيقية! 🎉

---

## ✅ قائمة التحقق النهائية

- [ ] تم إنشاء مشروع Supabase
- [ ] تم تشغيل ملف `001_initial_schema.sql`
- [ ] تم تشغيل ملف `seed.sql` (اختياري)
- [ ] تم إضافة متغيرات البيئة في Vercel:
  - [ ] NEXT_PUBLIC_SUPABASE_URL
  - [ ] NEXT_PUBLIC_SUPABASE_ANON_KEY
  - [ ] SUPABASE_SERVICE_ROLE_KEY
  - [ ] NEXT_PUBLIC_APP_URL
- [ ] تم إعادة نشر التطبيق في Vercel
- [ ] تم إنشاء حساب مستخدم والتسجيل
- [ ] التطبيق يعمل والصفحات تعرض البيانات ✅

---

## 🎯 التطوير المحلي (Local Development)

إذا أردت تشغيل التطبيق على جهازك:

```bash
# 1. انسخ الملف
cp .env.example .env.local

# 2. عدّل .env.local وضع قيم Supabase الخاصة بك

# 3. ثبّت المكتبات
npm install

# 4. شغّل التطبيق
npm run dev

# 5. افتح المتصفح:
http://localhost:3000
```

---

## 📞 الدعم والمساعدة

إذا واجهت أي مشاكل:

1. افتح issue على GitHub
2. أو راسلني مباشرة مع:
   - لقطة شاشة للخطأ
   - رسائل الخطأ من Console (F12)
   - Vercel function logs

---

**Good luck! 🚀**

إذا اتبعت هذه الخطوات بالترتيب، يجب أن يعمل التطبيق بشكل كامل!
